default:
    suites:
        default:
            contexts:
                - Fulll\Features\FleetContext

# Critical scenarios replayed against the real PostgreSQL infrastructure.
integration:
    suites:
        default:
            filters:
                tags: '@critical'
            contexts:
                - Fulll\Features\FleetContext:
                      driver: postgres
